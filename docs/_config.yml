title: tito-pdf
description: Standalone, deterministic PDF/DOCX → Markdown (+ optional tables)
repo_url: https://github.com/averriK/tito-PDF
issues_url: https://github.com/averriK/tito-PDF/issues

permalink: pretty

markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge

defaults:
  - scope:
      path: ""
    values:
      layout: default
